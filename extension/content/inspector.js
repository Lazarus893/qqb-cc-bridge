// inspector.js — content script reserved as a fallback channel for cases
// where chrome.debugger is unavailable (e.g. user revokes permission). Not
// loaded automatically yet; M6 plugs it in via chrome.scripting.executeScript.

// Currently a no-op. Keeping the file present for future expansion.
console.debug('[qqb] inspector loaded')
